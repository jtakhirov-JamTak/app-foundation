# Decision log

Why the foundation is built the way it is, newest first. Entries are kept verbatim,
including the measurements that settled them — a rejected optimization is only useful
if the evidence against it survives. Current-state architecture lives in
[`ARCHITECTURE.md`](../ARCHITECTURE.md); defects and their regression tests live in
[`FIX_LOG.md`](FIX_LOG.md).

### 2026-08-02 — The template is frozen until the first port-back, and this entry is the exception that records it

**No further template change until a derived app ports one back under playbook §4.** The freeze holds from the commit carrying this entry forward. Only §4 port-backs from derived apps may change this repository; a change with no app behind it is the speculative improvement §4 already forbids, and two were made this week on thinning justification — the five S9 blind-test defects, which had two independent reproductions each, and then the app-owned log split, which had a structural argument and one attribution field that had to grow a second value. That is the trend the freeze stops.

**This entry is itself a freeze exception, taken deliberately and for one reason: the constraint was living in machine-local session memory rather than in the repository.** A rule that only one agent on one machine can see is not a rule the next reader can follow — and the freeze is exactly the kind of thing a future session would otherwise "fix" on finding `main` ahead of its tag. Recording it costs one docs-only commit; not recording it costs the freeze.

**References.** `v1.1.0` = `0ca4535`. `main` stood at `44dc5a3` immediately before this entry — one commit ahead of the tag, deliberately untagged. That commit is the `docs/APP_FIX_LOG.md` / `docs/APP_DECISIONS.md` split, which ships officially in v1.2 once app #2 has validated the shape. `v1.0.0-rc` is retained as the historical RC snapshot. App #2 scaffolds from `main` HEAD rather than from the tag, recording `foundationVersion: "v1.1.0"` and `createdFromCommit` = that HEAD sha in its `foundation.json` — provenance covers the untagged delta, and `START_NEW_APP.md` step 7 already produces exactly that pair without amendment.

**The expected contents of v1.2, so the backlog is a record rather than a memory.** Password recovery and production observability — uptime monitoring, a dependency-aware health check, an error-sink interface — built in app #2 first and ported back per §4. Validation of the log-split shape, plus whatever `START_NEW_APP.md` and playbook §4 need adjusting to once a real app has used it. And a decision on `FIX_LOG` scope: whether the log is code-only or covers **any** defect a scaffolded app hits, written as a playbook §10 line rather than left to precedent. S9's defects #4 and #5 were documentation and got no rows on instruction; if the answer is "any", those two rows are backfilled at that point. Fourth, markdown link and anchor checking enters `npm run verify`, by the same §4 route — validated in app #2 first, not added here. There is no link checking in this repository today: no markdownlint, no remark, no lychee, nothing. What that leaves unguarded is specific rather than theoretical — this log's headings are dated slugs, so `### 2026-07-29 — experimental.inlineCss measured and rejected` anchors as `#2026-07-29--experimentalinlinecss-measured-and-rejected`, with a **double** hyphen where the em dash was and the punctuation stripped, and `ARCHITECTURE.md` links into exactly those. Rename a heading and the link rots silently: nothing renders an error, nothing fails a build, the reader just lands at the top of the page. The interim checker every docs change since S8 was verified with is preserved at `~/dev/tooling-holding/check-anchors.mjs`, outside this repo because the freeze forbids putting it in — its header carries the negative-control sequence that proves it can actually fail, which is the only thing that makes a green run mean anything.

**One tooling note that will outlive the memory of it.** The defect-#1 fix introduced the first **runtime** `@/` path-alias import inside `e2e/` — `e2e/auth-shell.spec.ts` and `e2e/navigation-mobile.spec.ts` now import `src/lib/navigation/routes.ts` for real, where the only prior `@/` use in that directory was `import type`, which erases before Playwright ever sees it. It resolves today, proven by a green two-engine `npm run test:e2e`. If a future Playwright, TypeScript, or Next bump breaks the e2e suite with module-resolution errors, this is the first suspect, and the fallback is the relative form already used at `e2e/global-setup.ts` rather than reverting the route export.

**Evidence note: the S9 scaffolds are machine-local and unarchived.** `/home/jtakh/af-v11-notes-a1` (~154 MB) and `/home/jtakh/af-v11-weighins-b1` (~156 MB) hold the two blind runs; `/home/jtakh/app-foundation-notes-baseline-iNTQTL` (~164 MB) is S0's. None is committed, tarballed, or backed up, and all three are deletable at any time — the same fragility this log criticised S0 for. The measurements they produced are transcribed in the entry below, which is what has to survive; the trees themselves are corroboration. If they are ever archived, name the tarball location here.

### 2026-08-02 — v1.1 blind-tested against S0: SQL analytics edits reached zero, and the release was held

**The S0 baseline was never recorded in this repository, so the first act of measuring v1.1 was writing S0 down.** It survived only as a throwaway clone at `/home/jtakh/app-foundation-notes-baseline-iNTQTL` and a transcript under `~/.claude/projects/`, neither of which is durable storage. The figures below are reconstructed from that transcript's timestamps and the surviving working tree, not contemporaneous notes, and they are recorded with that caveat rather than presented as measurements taken at the time. S0 ran a "Notes" feature task blind against commit `3f04aa7` on 2026-07-30.

**Two blind runs were executed against `58a5fd7`: the S0 Notes task verbatim, and a new Weigh-ins task written in the identical shape.** Each ran in its own disposable clone scaffolded per `START_NEW_APP.md` — `/home/jtakh/af-v11-notes-a1` and `/home/jtakh/af-v11-weighins-b1`, both left on disk unmerged so the evidence stays locatable. Weigh-ins deliberately exercises different constraint machinery than Notes: a required date and numeric weight with range CHECKs, one row per user per date, and no future dates, against Notes' case-insensitive title uniqueness among active rows. Blindness here means a fresh agent context given only the task prompt; S0's blindness was a separate interactive session, and the difference is recorded rather than glossed.

| Metric                                 | S0 Notes         | v1.1 Notes       | v1.1 Weigh-ins   |
| -------------------------------------- | ---------------- | ---------------- | ---------------- |
| Task wall-clock                        | 50 min 23 s      | 20 min 42 s      | 21 min 53 s      |
| Feature files created                  | 22 + 1 migration | 15 + 1 migration | 17 + 1 migration |
| Foundation files touched, raw          | 0                | 6                | 6                |
| …excluding sanctioned extension points | 0                | 3                | 3                |
| **SQL analytics edits**                | **2**            | **0**            | **0**            |
| Gate steps passed / failed / skipped   | 9 / 0 / 3        | 9 / 0 / 0        | 9 / 0 / 0        |

**The headline number moved the wrong way on purpose, and the number that matters moved the right way.** S0's zero came from `_feature/analytics-extension.ts` declaration merging, a mechanism the 2026-08-01 zod-catalog change deleted on purpose — `catalog.ts` now says in as many words that declaration merging cannot reach zod-derived types and the catalog is extended by editing it. So a raw score of 6 is the designed cost, not a regression, and the sanctioned extension points it includes are `src/lib/analytics/catalog.ts`, `src/lib/analytics/screen-registry.ts`, and the regenerated `src/types/database.ts`. What v1.1 actually claimed was the other column: S0's migration replaced `public.analytics_event_valid()` **wholesale** and dropped and re-added `events_event_name_allowlist`, because every migration adding an event had to re-declare the entire allowlist. Both v1.1 migrations touch `analytics` and `events` **not at all** — grep returns nothing. That claim is now measured rather than asserted, twice, by agents that were never told it was the point.

**Both runs also halved the clock and ran the gates S0 could not.** S0 skipped `db:reset`, `db:test`, and `test:e2e` — Docker was unreachable and Chromium was missing `libnspr4` — so its "zero gate failures" covered nine of twelve steps. Both v1.1 runs ran all of them green, including both Playwright engines against a live local stack, and `npm run verify` was re-run independently in each scaffold afterwards rather than trusting either agent's summary.

**The release was held, because the same three foundation files were edited by both runs for the same reason, and that is a defect the template owns.** Adding one primary route breaks two foundation e2e specs: `e2e/auth-shell.spec.ts` asserts `.skeleton` `toHaveCount(2)`, a literal that any derived app's first feature invalidates, and `e2e/navigation-mobile.spec.ts` asserts Settings is the route adjacent to `/`. Two independent agents hit both, and each rewrote a foundation test to accommodate its own feature — precisely the failure mode where a template teaches derived apps to edit the gates. A third, found while confirming the second: `src/lib/navigation/use-primary-swipe.test.ts` asserts `["/", "/settings"]` has length 2 and no duplicates — a local literal that never imports `PRIMARY_ROUTES` (which `app-shell.tsx` does not export), so it cannot fail and asserts nothing about the app. That is the vacuous-gate class the README points at `FIX_LOG.md` to explain. Two more surfaced from the scaffold step itself and were independently reported by both agents: after the example feature is deleted, `ARCHITECTURE.md`'s _Example-only_ section and `CLAUDE.md`'s canonical-example pointer both name paths that no longer exist, and `START_NEW_APP.md`'s closing rename check — documented as "must return no results" — returns 26 hits across 6 files in a correctly scaffolded app.

**None of these are fixed here, and the v1.1.0 tag is not cut, because the sequence's own rule is that no tag ships over a known unreported defect.** They are template defects found by throwaway apps, which is exactly the input playbook §4 is written for, and the fix belongs in a change that carries regression tests and `FIX_LOG.md` rows rather than in a release commit. The measurement above stands on its own and does not need re-running once they are fixed: nothing in the defect list touches the analytics or migration path that the zero-SQL-edits result depends on.

**All five were fixed immediately after, and the recorded figure of 3 is now conservative rather than current.** Two of those three files — `e2e/auth-shell.spec.ts` and `e2e/navigation-mobile.spec.ts` — were edited only because the gates demanded it, and both now derive from `src/lib/navigation/routes.ts` instead. The post-fix expectation for a feature of this shape is **1**: `app-shell.tsx`, appending one entry to the nav registry. The blind tests were deliberately **not** re-run to collect that number. Re-running would measure the fix rather than the release, and the figure that carries the v1.1 claim — zero SQL analytics edits — cannot move, because nothing in the fix touches the catalog, the migration path, or `public.events`. A number recorded high and explained is worth more than a number re-measured until it flatters.

**Fixing them is a template change made ahead of app #2, and the exception is recorded here rather than taken quietly.** The standing rule is that template changes come only from lessons in shipped apps, and speculative improvements are forbidden. These are not speculative: playbook §4 step 10 requires a bug found in the template to be reproduced in a throwaway app before the template is changed, and that is precisely what happened — two independent throwaway apps hit the same two gates and made the same edits before any fix was written, and the vacuous test was confirmed by reading the code it failed to import. The rule exists to stop imagined improvements, not to make reproduced defects wait for an app that has not been built. Had these shipped as v1.1.0, app #2's first feature would have paid all five costs on day one.

### 2026-08-02 — The CSP ships without script-src or default-src, and that is a recorded gap

**The policy in `next.config.mjs` restricts navigation and embedding, not script execution.** It ships `base-uri 'self'; form-action 'self'; frame-ancestors 'none'; object-src 'none'` — four directives and no `default-src`, so every directive not named is unrestricted, `script-src` among them. Until this entry the absence was undocumented rather than decided: a repo-wide grep for `script-src`, `nonce`, `CSP`, or `Content-Security` returned exactly one hit, the header key itself, and the gap appeared in neither _Explicitly Deferred_ nor here. Playbook §8 requires one or the other, so this was an oversight wearing a decision's clothes.

**What blocks the strict form is nonces specifically.** A nonce-based `script-src` needs a fresh per-response value stamped into both the header and every inline script tag on the page. The safe shell is precached by the service worker and served from cache — one stored document answering many navigations, so there is no per-response anything to stamp. Next's inline bootstrapping compounds it: the framework injects inline script this app neither authors nor can enumerate. A nonce policy would therefore either break the precached shell or fall back to `'unsafe-inline'`, which buys nothing while looking like it does.

**That argument is about nonces and does not generalize to all script restriction.** Two paths are deliberately left open rather than closed. A hash-based `script-src` is compatible with a statically precached document, because a hash is a property of the content and not of the response. A `Content-Security-Policy-Report-Only` rollout costs nothing at runtime and would report what an enforced policy would have broken before anything is enforced. Neither was measured, so neither is rejected — what is recorded here is why the strict version does not ship today, not a finding that the problem is unsolvable.

**What does ship still does real work, and each shipped directive is nonce-free for the same reason.** `frame-ancestors 'none'` with `X-Frame-Options: DENY` behind it blocks framing; `base-uri 'self'` blocks a `<base>` injection from re-pointing every relative URL on the page; `form-action 'self'` stops a form posting to an attacker origin; `object-src 'none'` removes the plugin surface. None of the four depends on a per-response value, which is exactly why they were shippable when `script-src` was not.

**HSTS is not set by this repository, and that is not the same as HSTS being absent in production.** No `Strict-Transport-Security` header exists anywhere in the code, and hosting platforms commonly add one at the edge. Verify it on the deployed host at launch — `curl -sI https://<host> | grep -i strict-transport` — rather than assuming either that the platform supplies it or that nothing does. No test asserts any of these headers today; a header assertion is the natural first step for a derived app that tightens this.

### 2026-08-02 — Password recovery and production observability are built in app #2 and ported back as v1.2

**Four gaps are deliberately built in the next app first, not here: password recovery, uptime monitoring, a dependency-aware health check, and an error-sink interface.** The route is playbook §4 — build and fix it in the app where the requirement actually appeared, extract the smallest product-neutral version, apply that to the template with its regression test, add a `FIX_LOG` row, tag. Building them here instead is what §4 closes with in as many words: template changes come only from lessons in shipped apps, and speculative improvements are the analysis loop wearing a maintenance costume. A recovery flow written against no real mail provider, and an error-sink interface written against no real vendor, would both encode guesses that the first real integration then has to unpick.

**The condition that binds it: app #2 must not launch to real users without the LAUNCH BLOCKERS checklist affirmed in `/deploy-check`.** That checklist is not a coincidence — five of its six printed items _are_ this deferral, itemized. `release:verify` prints them on both the pass and fail paths without touching the exit code, and `/deploy-check` requires each line affirmed PASS/FAIL/SKIPPED with a reason, an unaffirmed line counting as FAIL. The deferral and the thing that catches the deferral are therefore the same list, which is the property worth preserving if either is ever edited.

**Enforcement is a mandatory human checklist until v1.2, and that is the weak link, named rather than hidden.** A checklist catches a launch; it cannot catch a build. What changes at v1.2 is that app #2 will have produced a real error sink and a real health check, and a template shipping those interfaces can fail a build that has no sink configured or no health route mounted — enforcement moves from a person remembering to a gate refusing. Until then, the person is the gate. Do not read the eventual code-level enforcement as a reason to relax the checklist before it exists.

**This entry is the mechanism that stops "explicitly deferred" from becoming "never."** A deferral carrying no owner, no target version, and no condition is indistinguishable from an oversight two sessions later, and the difference matters precisely because both look identical in the code. Owner: app #2. Version: v1.2. Condition: no launch to real users without the checklist affirmed. If v1.2 ships without these, that is a decision someone has to write down here and defend, which is the entire point.

### 2026-08-02 — v1.0.0 was never tagged; v1.0.0-rc is kept and v1.1.0 supersedes it

**There is no `v1.0.0` tag, and there never was.** `git tag -l` returns exactly one tag in the repository, `v1.0.0-rc` at `9a13bb7`, and `main` is 35 commits ahead of it. Two files nevertheless assert the version: `README.md` reads `Template version: v1.0.0` and `package.json` reads `"version": "1.0.0"`. Both name a release that no tag points at.

**`v1.0.0-rc` is retained, not deleted.** It is the historical snapshot of what the release candidate actually contained, and the 35 commits since are the argument for keeping it rather than for tidying it away — deleting the only tag would erase the one fixed point anything before now can be diffed against. Do not delete it when v1.1.0 lands.

**v1.1.0 — to be tagged in the S9 release sequence, which bumps every version source on the release commit itself — supersedes it.** Version strings are deliberately not bumped in this session. A version bump belongs on the commit its tag points at, so that `README.md`, `package.json`, and the tag agree at the moment of tagging instead of drifting between a docs session and a release session. Playbook §4 step 8 also requires `npm run release:verify` green before any tag, which is a heavier gate than this session runs.

**The drift being recorded is that nothing checks the version sources against each other.** `README.md` uses `v1.0.0`, `package.json` uses `1.0.0`, every `FIX_LOG` row carries `**Version:** 1.0.0`, and playbook §4 step 8 hardcodes `v1.0.0 → v1.0.1` as its worked example — four hand-maintained places, two formats, no mechanical comparison between any of them. The `FIX_LOG` rows stay as written: history describes the code as it was, and a superseding entry does the correcting. If S9's bump proves annoying, a check comparing `README.md`'s line to `package.json`'s field is the smallest fix, and it belongs in `npm run verify` rather than in a person's memory.

### 2026-08-02 — Server failures leave a controlled trace; detection and alerting stay deferred

**Before this, a 500 left nothing behind.** `apiErrorFromUnknown` returned a sanitized `{code, request_id, recoverable}` to the client and dropped the cause; `grep -rn 'console\.' src/` returned zero hits. Analytics only fire from _client_ error boundaries, so a server-side throw was invisible everywhere. It now emits one structured stderr line on any status ≥ 500, and `/api/events` logs its own `EVENT_WRITE_FAILED` inline because that 503 is returned via `apiError` directly and never reaches the shared logger. **Scope is deliberately traceable, not noticed** — no health endpoint, no uptime monitor, no error sink. Those remain required before app #2 launches, and ARCHITECTURE's _Explicitly Deferred_ entry was narrowed rather than deleted to say so.

**There is no message field, and its absence is the design.** Vendor and Postgres messages embed user data (playbook §6) — a Supabase or Stripe error routinely echoes the request body, and a Postgres `message`/`details`/`hint` can quote row content. The line carries `level`, `request_id`, `code`, and `error_type` only; the events route adds the Postgres SQLSTATE (`pg_code`) and nothing else. A code comment names the one sanctioned extension point: if a message is ever needed it enters through an explicit `safeLogMessage` field a caller consciously populates with a string it owns, never `error.message`. That field is not added now, because adding it speculatively is exactly what makes the next person reach for `error.message`.

**`SESSION_UNAVAILABLE` logs at `warn`, not `error`, and that split encodes a known conflation.** The rule started as a flat `status >= 500`. Reading `require-user.ts` against supabase-js showed why that was wrong: the only `AppError`s in production code are `UNAUTHENTICATED` (401) and `SESSION_UNAVAILABLE` (503), and an actually-expired session produces the **503**, not the 401 — `getClaims()` returns `{data: null, error: null}` when there is no session at all (→ 401, silent), but an expired or revoked _refresh_ token makes `getSession()` return an error, and an expired JWT makes `validateExp` throw `AuthInvalidJwtError`. Both land on 503. A flat rule would therefore emit an error line per request for every user whose session expired — an expected condition, on a PWA that background-syncs. Quiet-listing the code instead was rejected: it would go blind to a Supabase auth outage, which is the single most important server failure this trace exists to catch. `warn` keeps both. The residual problem is that one code still means two things — a single warn is an expired session, a sustained spike across many `request_id`s is a probable outage. **Splitting it into two codes is recorded here as a candidate for app #2's observability work**, where an error sink can aggregate warn-rate rather than relying on per-line severity; it was out of scope for a change whose point was the minimum trace.

**`digest` is optional and `recordError` spreads it rather than passing it.** `assertSafeEventProperties` iterates `Object.entries` and rejects any non-scalar, so a present-but-`undefined` key throws — and `digest` is `undefined` for every purely client-side error, i.e. the common path. `{area, code, recoverable, digest}` would have thrown in all three error boundaries. `...(digest ? { digest } : {})` is the only shape that survives, mirroring `trackScreen`'s existing `referrer_screen` idiom. `privacy.test.ts` now locks that exact failing condition rather than the fix. The key itself needed no new safety argument: `digest` was already in `ACCEPTED_KEYS` and in the SQL vector, so `db:test` would have passed without proving anything — what was missing, and was added, is the `app_error_recorded` fixture _carrying_ a digest.

**The typecheck hook is Stop, not PostToolUse, and parses its input with node, not jq.** Per-edit typechecking flags intentionally incomplete intermediate states mid-refactor and burns context on noise; a turn-boundary check runs once a coherent batch has landed. Exit 2 blocks the stop and feeds stderr back, which is what makes it self-correcting rather than informational. The documented loop guard reads `stop_hook_active` — real, though it lives in the hooks _guide_ troubleshooting section rather than the reference — and the documented example uses `jq`. **`jq` is not installed here and is not guaranteed on any derived app's machine**, and a missing `jq` makes the guard's test silently never fire: precisely the silent-exit class non-negotiable #9 exists to prevent. Node 22 is already a hard repo requirement, so the guard parses stdin with `node -e` and falls back to `false` on a parse failure — failing toward running the check, never toward skipping it quietly. Claude Code independently overrides a Stop hook after 8 consecutive blocks, so there is a backstop beneath the guard. `npm run verify` remains the hard gate; the hook is advisory speed. Accepted cost: a turn that only answers a question still pays the typecheck latency and can be blocked by breakage it did not introduce. **If it proves noisy in real use, delete it then, with that evidence** — not pre-emptively.

**LAUNCH BLOCKERS prints without failing, and `/deploy-check` is where it binds.** `release:verify` prints the checklist on both the pass and fail paths but never changes the exit code: these are human steps no script can prove, and gating automation on them would only teach people to skip the gate. Printing is not affirming, so `/deploy-check`'s human section now requires each line affirmed explicitly — PASS/FAIL/SKIPPED with a reason — before a derived app's first real users, with an unaffirmed line counting as FAIL.

### 2026-08-02 — Twenty-eight commands became thirteen; ARCHITECTURE keeps the numbers, DECISIONS keeps the narrative

**`.claude/commands/` went from 28 files to 13, and the cost being paid down was drift, not disk.** The reviewer-conventions preamble was copy-pasted into 12 files, `/full-review` and `/full-audit` and `/deploy-check scope=full` each re-described the same fan-out from a different angle, and five `/add-*` files repeated the same discover-first pass. Every duplicated paragraph is a place where two files can disagree and nothing catches it. Three merges absorbed seventeen files: `/add` takes a layer argument (endpoint/page/webhook/feature) over one shared discover pass; `/review` takes a mode (`changes`/`pipeline`/`repo`/`debt`); `/audit` takes an area (access/privacy/perf/deps/mobile/a11y/observability/db/ci).

**Four commands stayed standalone, each for a stated reason, because "fewer files" is not the goal — "no file that can silently disagree with another" is.** `/add-table` is the security boundary and must not be skimmable as one section of a longer file. `/grill` is the only command carrying a _method_ (construct a concrete input that breaks the code) rather than a checklist, and its category-12 omission hunt is duplicated nowhere. `/ai-prompt-review` has zero drift risk today — the repo has no AI surface at all, no `anthropic`/`openai`/`PROMPT_VERSION`/`generator_version` anywhere in `src/` or `supabase/` — while encoding VERSION-GUARD reader/writer symmetry that derived apps adding AI will need. `/deploy-check` was reworked rather than merged: it is now the release home, wrapping `npm run release:verify` and then the human checklist the mechanical gate cannot cover (rung 6 of playbook §2 — real phone, production build), and its subscription-lifecycle section is unique.

**The accepted regression is context cost.** `/audit db` now loads all nine areas (~230 lines) where `db-check.md` was 101, and `add.md` historically carried a note saying the `/add-*` files were split precisely so invoking one layer didn't load the other four. That note is now false and was removed. Both merged files instruct "read **only** that section," which is mitigation, not a fix. If a future session finds an area being skimmed because the file is long, splitting that one area back out is the correct response — not reverting the merge.

**`.claude/REVIEWER_CONVENTIONS.md` folded into `ENGINEERING_PLAYBOOK.md` §11, which fixed a citation that had never resolved.** Seventeen citations across nine command files read `.claude/REVIEWER_CONVENTIONS.md §6`; that file was a flat five-bullet list with no numbered sections, so §6 never existed. All seventeen now read `.claude/ENGINEERING_PLAYBOOK.md` §11, which does. The playbook's §9/§10 inversion (§10 appeared before §9) was corrected in the same pass.

**ARCHITECTURE.md's Performance budgets shed ~1.2 KB of narrative that was already verbatim in this file.** The budget numbers stayed; what left was measurement prose — the 95/395/421 ms TBT batch, the 2143–2198 ms LCP spread, the ~130 ms preconnect drift, 268–408 ms across 19 runs, the ~6× headroom, the field-number-not-local-calibration rule, the `router.replace()` soft-navigation mechanism. Each clause was diffed against the entry it linked to before deletion; all but one were already present word-for-word, so this was removing duplication rather than relocating it. The exception ("re-adding `/` to `lighthouserc.cjs` once it is a real page…") was appended verbatim to the 2026-07-30 entry above. Playbook §10 already said ARCHITECTURE describes only what is true now; it had drifted into holding the record as well.

**`docs/FIX_LOG.md` keeps its `/dep-audit` mention as written**, following the precedent set by the 2026-07-30 entry below: history describes the code as it was, and a superseding entry does the correcting. The post-change grep for deleted command names therefore returns exactly one hit, in that file, by decision rather than by oversight.

### 2026-08-02 — Expensive proofs moved off the per-PR path onto a release gate

**Per-PR gates are now cheap and deterministic; expensive proofs run on `npm run release:verify`.** Three checks were demoted, and each was replaced rather than dropped — the point is where they run, not whether.

**`verify:sw-version-bust` is deleted, and `check:sw` is strictly stronger than what it replaced.** The script built three times (two synthetic versions plus a restore build) to prove cache names track `NEXT_PUBLIC_APP_VERSION`. One build proves it: `src/app/sw.ts` derives `cacheId` as `` `${appId}-${appVersion}` `` from two inlined literals, so the bundler constant-folds it to a single string in `public/sw.js`. `check-service-worker.mjs` now asserts that exact concatenation for the env the build ran with, and CI builds with a distinct `ci-<sha>` version every run, so a stale or hardcoded id cannot slip past. Verified by planting the failure, not by reading the code: built at `proof-v1`, then checked at `proof-v2` — exit 1 naming the expected id; checked with `NEXT_PUBLIC_APP_VERSION` unset — exit 1 refusing to pass vacuously. The old check compared `appId` and `version` as two independent substrings, which a hardcoded cache name containing both would have satisfied. `NEXT_PUBLIC_APP_VERSION` is now required by `check:sw` rather than optional; every caller already sets it. This resolves the `verify:sw-version-bust` simplification (P1) item formerly in **Explicitly Deferred** on its own stated terms — a cheap deterministic test, retained `e2e/service-worker.spec.ts` coverage, _and_ a release-only check.

**Lighthouse is release-only.** `lhci autorun` plus `check:lighthouse-url` are now `npm run perf:lab`, invoked by `release:verify`. Lab numbers are the least deterministic signal in CI and the slowest; the 2026-07-29 entry below already records that local TBT spans 95/395/421 ms in a single 3-run batch. The `check:lighthouse-url` guarantee from [`FIX_LOG.md`](FIX_LOG.md#2026-07-29--the-lighthouse-gate-never-measured-the-app-shell) is unchanged — it still runs immediately after `lhci`, still refuses to pass on an empty `.lighthouseci`. The fix-log entry says it is "wired into CI directly after `lhci`"; that stays as written and describes the code as it was, and this entry supersedes it.

**Per-PR e2e runs mobile-chromium only.** The accepted risk is recorded in ARCHITECTURE.md Trade-offs: a Safari-specific regression can sit on `main` between releases. Both engines run before any tag.

**The `performance-pwa` job is deleted outright, not rebuilt.** With lhci and the triple-build gone it held nothing unique: `quality` already runs `npm run build` then `check:sw` under the identical workflow env, and `mobile-e2e`'s Playwright run already includes `e2e/service-worker.spec.ts` under mobile-chromium — against a build made with real local-Supabase credentials, which is strictly more realistic. Keeping it would have cost a fourth runner and a duplicate build per PR for coverage that already existed.

**`release:verify` skips `verify:example-removal` on the example feature's absence rather than instructing derived apps to edit the script.** A doc note telling a scaffolder to delete a step is exactly the manual dependency this gate exists to remove; a forgotten edit would leave a derived app's release path permanently red.

### 2026-08-01 — Event semantics live in one zod catalog; the database enforces only generic invariants

**Event semantics had three definitions and now have one.** They were TypeScript interfaces in `catalog.ts`, a loose runtime schema in `schemas.ts` that accepted any `^[a-z][a-z0-9_]{0,63}$` name with any scalar record, and an 80-line `analytics_event_valid()` plpgsql function that every migration adding an event had to re-declare in full — `202607210002_example_records.sql` copied 86 lines of it to add one event, and `check-analytics-contract.mjs` grepped migrations to keep the copies in sync. `catalog.ts` is now zod-first: schemas define the enums and each event's `.strict()` properties, `eventPropertySchemas` keys the map, and `EventName`, `EventProperties`, `ScreenName` and the `/api/events` discriminated union are all `z.infer`red from it. The map rather than a flat union is what preserves per-event property typing for `trackEvent`; `catalog.test.ts` holds a never-invoked function with one valid call and one `@ts-expect-error` cross-event call, so `npm run typecheck` — not a reviewer — is what keeps that mapping.

**The database keeps only what survives any catalog:** name format, object-typed properties, scalar values, 4 KiB, and `analytics_properties_safe()`. The trade is deliberate — the DB stops rejecting a semantically wrong event, so the write path is closed instead of trusted: `events` has no RLS policy and no client insert grant, and `/api/events` writes with the service role after `requireUser()`. A client that bypasses TypeScript now reaches a route that parses the union, not a table.

**The sensitive-key regex rejects `screen_name` on purpose.** Matching on word boundaries (`(^|_)name(_|$)`) rather than bare substrings lets `subtitle`, `notes_count` and `hostname` through, which the old substring rule blocked. It still rejects every `*_name` segment including technical-sounding ones, because enumerating the acceptable variants is an unwinnable blocklist — `child_name`, `partner_name`, `doctor_name`. The answer is to rename the key at catalog-design time (`screen`, `metric`, `code`). The risk that inverts is a false positive rejecting a legitimate event, so pgTAP inserts one valid instance of every catalogued event: a future policy change that breaks one fails `db:test` instead of failing silently in production. The same accept/reject vectors are asserted in `privacy.test.ts` and in SQL — that pairing is the cross-language contract.

**Declaration merging was dropped, not replaced.** `keyof typeof` over a const map and `z.infer` enums cannot be augmented, so `_feature/analytics-extension.ts` and `registerScreenPath` are gone and the catalog is extended by editing it. The example feature's entries sit in `EXAMPLE-ONLY` blocks in `catalog.ts` and `screen-registry.ts`, which `verify:example-removal` strips in a throwaway copy.

**That hand-maintained `MARKED_FILES` list is only as safe as what fails when it is wrong, so each direction has its own guard — established by planting each mistake and confirming the exit code, not by reading the script.** A marker in a file nobody listed is caught by the repo-wide marker scan over `src/`, `supabase/` and `e2e/` (exit 1). A listed block that _loses_ its markers splits in two: if the leftover dangles it is caught by `typecheck` (exit 2) — an orphaned `eventPropertySchemas` entry referencing a stripped schema, a `screen-registry` tuple the stripped enum no longer admits — but a **self-contained** leftover such as `"example"` in an enum compiles, tests and ships silently, so `FORBIDDEN_LITERALS_AFTER_STRIP` checks the quoted literals directly (exit 1). It is token-specific rather than a repo-wide `/example/i` scan because the pgTAP fixtures and e2e specs legitimately use `@example.invalid` addresses. SQL needs no marker at all: the example's analytics fixture row lives in `src/app/(app)/(example-feature)/_tests/example-rls.sql`, deleted wholesale with the folder — structural co-deletion, which is why it is preferred over a guard wherever the layout allows it. That matters here because `verify:example-removal` cannot run `db:test` (no live database), so a marked SQL block would have had no guard in either direction.

### 2026-07-30 — WSL2/Linux is the only supported development environment

WSL2/Linux is the only supported development environment; native-Windows support is dropped. The four `shell: process.platform === "win32"` branches (Fix Log 2026-07-22/24), the `junction` symlink fallback, and the PowerShell command forms in `.claude/commands/` are removed — all were no-ops on Linux, so this changed no behavior on the supported platform. The fix-log entries in [`FIX_LOG.md`](FIX_LOG.md) stay as written and describe the code as it was; this entry supersedes them. What survives is platform-independent and still enforced by non-negotiable #9: spawns surface `result.error`, so a gate can never exit silently. `.gitattributes` (`* text=auto eol=lf`) is unchanged — it protects any clone regardless of the developer's `core.autocrlf`, including a WSL repo on `/mnt/c`.

### 2026-07-30 — Unauthenticated cold start is measured in Playwright

**Unauthenticated cold start is measured in Playwright, closing the gap that dropping `/` from lhci opened.** `e2e/performance.spec.ts` loads `/` with `/api/session` mocked unauthenticated, waits for the redirect and the sign-in form, and asserts LCP ≤ 2.5 s. The measurement is valid across two URLs because `AuthBoundary` uses `router.replace()` — a soft navigation, so the document survives and LCP neither resets nor rebases its timestamps. That is not an assumption: instrumenting the observer showed exactly two candidates per run, the pre-redirect shell text at ~132–144 ms and the sign-in subtitle at ~276–296 ms, so the winning entry is post-redirect content timed from the original navigation. Measured 268–408 ms across 19 local runs. **The budget is the 2.5 s field number, not a local calibration** — the same constant already used by the authenticated cold-launch test and the budgets list, so it carries one meaning everywhere; a machine-fitted threshold would read as a tighter guarantee than a shared ubuntu runner can keep. Tighten only against numbers measured on that runner. Note the ~6× headroom is against an unthrottled Playwright profile and is not comparable to a throttled Lighthouse number for the same journey (the retired lab figure was ~3.1–3.4 s). Re-adding `/` to `lighthouserc.cjs` once it is a real page (see `START_NEW_APP.md`) adds lab-profile coverage but is no longer the only way to measure this.

### 2026-07-29 — Local Lighthouse TBT is not reliably measurable

Lighthouse TBT is not reliably measurable on a loaded developer machine. Observed spread within a single 3-run batch: 95 / 395 / 421 ms on `/`. LCP by contrast is stable (`/sign-in` spanned 2143–2198 ms across 5 runs), so LCP A/B tests are trustworthy locally and TBT ones are not. Compare arms back-to-back in one session — between-batch drift of ~130 ms was observed on LCP for builds that differ only by a preconnect tag. The `/` TBT budget has drifted from the 153 ms median recorded 2026-07-24 to ~300 ms; cause not established, and it predates the preconnect guard (the parent commit measured worse). Settle it on a quiet runner before recalibrating the budget.

### 2026-07-29 — experimental.inlineCss measured and rejected

**`experimental.inlineCss` was measured and rejected — do not re-attempt on LCP grounds.** `/sign-in` LCP is ~96% render delay (TTFB 454 ms · load delay 0 · load time 0 · **render delay 1566 ms**), and the LCP element is the static subtitle `<p>` in `src/app/(public)/sign-in/page.tsx` — text already present in the prerendered HTML, with no image or webfont to fetch. Inlining the stylesheet removes the only render-blocking resource, and it does improve **FCP by ~70 ms** (767 → 696 ms on `/sign-in`, 773 → 704 ms on `/`; both arms n=5, same session). **LCP does not move:** 2166 → 2161 ms, inside the ~55 ms within-batch spread. The LCP paint is gated by main-thread script evaluation (234 ms, of which `webpack-*.js` is 143 ms of scripting), not by stylesheet arrival — the 154 ms Lighthouse charges the CSS was never the binding constraint. `/` LCP also measured 274 ms worse (3144 → 3418 ms); the distributions overlap (3090–3259 vs 3172–3597) so that is suggestive rather than established, but there is a real mechanism: `/` client-redirects to `/sign-in`, so inlining ships the full 15.6 KiB of CSS in **two** documents instead of letting the second reuse the cached stylesheet. Not adopted — FCP is not a budgeted metric here, and a permanent `experimental.` flag in the template is not worth an unbudgeted gain against a possible regression on a budgeted one. The remaining `/sign-in` LCP lever is that main-thread work, which is a profiling job, not a config flag.

### 2026-07-28 — browserslist is documentation, not an optimization

`browserslist` in `package.json` records the supported floor (Chrome/Edge 111, Firefox 128, Safari/iOS 16.4) — the same floor Tailwind v4 already imposes through its generated CSS, so it excludes no browser that could render the app. **It is documentation, not an optimization:** builds before and after were byte-identical (161.8 KiB gzip across 12 chunks; `polyfills-42372ed130431b0a.js` unchanged at 112594 B). Next 16 emits its `nomodule` polyfill chunk regardless. Do not cite this key as a bundle-size measure. **Root cause of the Lighthouse `legacy-javascript` flag, measured 2026-07-29:** the flagged shims in root-shell chunk `838` (`Array.prototype.at/flat/flatMap`, `Object.hasOwn`, `Object.fromEntries`, `String.prototype.trimStart/trimEnd`) are webpack module `8456`, byte-for-byte `next/dist/build/polyfills/polyfill-module.js` — Next framework code injected unconditionally into the client entry, **1380 bytes raw**, not SWC transpilation output. No compiler target removes it; the audit's "~12 KiB" is its whole-chunk savings estimate, not the polyfill's size. The es-shims in the lockfile (`string.prototype.trimstart`, `object.fromentries`, `array.prototype.flat`) are dev-only via `eslint-config-next` and are not in the client bundle. Removing this would mean aliasing a framework-internal path in webpack config — unsupported, and out of proportion to a sub-KiB gzip saving. Do not re-attempt on bundle-size grounds.

### 2026-07-28 — Supabase preconnect in the root layout

The root layout preconnects to the `NEXT_PUBLIC_SUPABASE_URL` origin. Every client-side Supabase call sits behind a dynamic import (session-provider's auth listener and sign-out, the sign-in form's submit), so the browser would otherwise discover the origin only when the first auth request fires; the preconnect warms DNS/TLS at first paint instead. `crossOrigin="anonymous"` is required — supabase-js issues CORS fetches without cookies, and a mismatched mode opens a connection the real request cannot reuse. The origin is baked at build time, like every other `NEXT_PUBLIC_` value. This targets auth-request latency, not LCP. `preconnectOrigin` (`src/lib/env/preconnect-origin.ts`) suppresses the tag for `localhost`/`127.0.0.1`/`[::1]` and the `example.supabase.co` placeholder: a local stack has no DNS/TLS handshake to save, and the placeholder host does not resolve, so the browser would open a connection the real request can never reuse. It is a pure function so the rule is unit-testable without touching the module-scope `clientEnv` parse.

### 2026-07-22 — Timed throwaway scaffold; offline test scope

The timed throwaway scaffold removed hidden template identity from `.env.example` and CI; the final rename check now passes without extra undocumented edits. The offline browser test now verifies the cached root shell plus protected offline state rather than the separate unknown-route fallback.

### 2026-07-22 — Validated app identity and deployment mode

Validated app identity and deployment mode: `NEXT_PUBLIC_APP_ID` names caches and rate-limit namespaces from one rename point. `APP_ENV` distinguishes local/test builds from deployed production, where distributed rate limiting is mandatory. No library was added.

### 2026-07-22 — Lighthouse gates LCP/CLS and TBT; INP comes from web-vitals

Lighthouse gates LCP/CLS and TBT; actual INP is collected by `web-vitals` and checked with Chromium Event Timing because Lighthouse has no deterministic scripted interaction audit.

### 2026-07-22 — Literal scaffold validation

Literal scaffold validation made feature test/type discovery generic, removed cache-prefix assumptions, and expanded clean-removal verification.

### 2026-07-21 — The example is one route-group folder plus one migration

Example is one route-group folder plus one migration.

### 2026-07-21 — Node.js 22

Node.js 22 because current Supabase JS no longer supports Node.js 20.

### 2026-07-21 — Serwist, SWR, static shell, route-local session refresh

Serwist, SWR, static shell, route-local session refresh, authenticated-only analytics.

### 2026-07-21 — Clean template rather than stripping a product repository

Clean template instead of stripping a product repository.
